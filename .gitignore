# OS cruft
Thumbs.db
.DS_Store

# Build output (the uploadable skill ZIP is regenerated by package-skill.ps1)
dist/

# Per-release notes written locally and uploaded as the GitHub release body
RELEASE_NOTES.md

# Python bytecode cache
__pycache__/
*.pyc

# Local inputs (user-specific, not source): game save files
*.sav
# ...but keep committed test fixtures (PII-checked sample saves)
!tests/fixtures/*.sav


.history
